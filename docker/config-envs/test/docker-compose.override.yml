services:
  php-sda:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_ENV: ${APP_ENV}
    environment:
      APP_ENV: ${APP_ENV}
#    volumes:
#      - .:/var/www/supplier-inventory-aggregator # for the ability to locally plug-and-play modify project files for development
    command: tail -f /dev/null
