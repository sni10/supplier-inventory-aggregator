services:
    php-sda:
        build:
            context: .
            dockerfile: Dockerfile
            args:
                APP_ENV: ${APP_ENV}
        environment:
            APP_ENV: ${APP_ENV}
            XDEBUG_MODE: ${XDEBUG_MODE}
#        volumes:
#            - .:/var/www/etl-edi-scraper
        command: tail -f /dev/null
